# MovieSearchEngine_Array
A program that created a database using a CSV file containing data about a multitude of movies and allows the user to search through it efficiently using the ArrayList Class
